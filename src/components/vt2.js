import React from "react"
import styled from "styled-components"

const StyledDiv = styled.div`
  margin-top: 2rem;
`

const VT2 = ({ children }) => <StyledDiv>{children}</StyledDiv>

export default VT2
