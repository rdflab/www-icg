import React from "react"
import styled from "styled-components"

const StyledDiv = styled.div`
  margin-bottom: 2rem;
`

const VS2 = ({ children }) => <StyledDiv>{children}</StyledDiv>

export default VS2
